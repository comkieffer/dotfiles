Git Merge Request Aliases

Work with GitLab merge requests from the command line.

$ [prgm]git mr[/prgm] [arg-value]origin 35[/arg-value]         # Checkout merge request #35 from origin (on GitLab)
$ [prgm]git view-mr[/prgm]              # Open current branch's MR in web browser (on Gitlab)
