Git Cleanup Aliases

Undo changes and clean up repository state.

$ [prgm]git undo[/prgm]                 # Undo last commit (keep changes unstaged)
$ [prgm]git gone[/prgm]                 # Delete local branches removed on remote
$ [prgm]git permission-reset[/prgm]     # Reset unstaged permission changes
