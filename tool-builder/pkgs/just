name="just"
version="1.51.0"

download() {
    fetch_or_clone \
        --repo    "https://github.com/casey/just.git" \
        --tag     "$version" \
        --src-dir "$(src_dir "$name")"
}

build() {
    build_with_rust \
        --src "$(src_dir "$name")" \
        --out "$(bin_dir)/$name"
}
