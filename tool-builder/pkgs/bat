name="bat"
version="v0.26.1"

download() {
    fetch_or_clone \
        --repo    "https://github.com/sharkdp/bat" \
        --tag     "$version" \
        --src-dir "$(src_dir "$name")"
}

build() {
    build_with_rust \
        --src "$(src_dir "$name")" \
        --out "$(bin_dir)/$name"
}
