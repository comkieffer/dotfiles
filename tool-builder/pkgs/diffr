name="diffr"
version="v0.1.5"

download() {
    fetch_or_clone \
        --repo    "https://github.com/mookid/diffr" \
        --tag     "$version" \
        --src-dir "$(src_dir "$name")"
}

build() {
    build_with_rust \
        --src "$(src_dir "$name")" \
        --out "$(bin_dir)/$name"
}
