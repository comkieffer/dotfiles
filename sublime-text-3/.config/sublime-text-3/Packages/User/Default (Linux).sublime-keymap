[
    { "keys": ["ctrl+i"], "command": "reindent", "args": {"single_line": true}},
    { "keys": ["ctrl+7"], "command": "toggle_comment", "args": { "block": false } },
    { "keys": ["ctrl+shift+7"], "command": "toggle_comment", "args": { "block": true } },

    // Open a new termnal with `Terminal View`
    { "keys": ["ctrl+shift+t"], "command": "terminal_view_open" },

    // Mimic IDEA behaviour (select next under cursor)
    { "keys": ["alt+j"], "command": "find_under_expand" },

]
